import { LayerManager } from "~/components/gradientNoise/layerManager";

const Home = () => {
    return (
        <main className="container mx-auto">
            <LayerManager />
        </main>
    );
};

export default Home;
